export function Statistics({ good, neutral, bad, total, positivePercentage }) {
  <div>
    <p>{good}</p>
    <p>{neutral}</p>
    <p>{bad}</p>
    <p>{total}</p>
    <p>{positivePercentage}</p>
  </div>;
}
